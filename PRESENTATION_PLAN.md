# 课程汇报主线、页序与答辩准备

**版本：** 2026-09-07

**适用范围：** 当前 ICU 数据研究的课程 presentation、Q&A 与组内排练

**材料来源：** 本仓库的研究报告、课程讲义和当前实验结果。评分比例和时间限制在最终提交前仍应以教师发布的正式说明为准。

## 一、评分机制怎样影响汇报结构

前期课程信息汇总显示，项目约占课程总评 40%，其中代码 20%，presentation 与 Q&A 15%，同伴评价 5%。项目评价看重的不是单一最高分数，而是能否形成一条可以追问和复核的推理链：

> 临床问题 → 数据 → 预处理 → 模型比较 → 评价指标 → 临床建议

最低任务要求包括至少两个预测任务、由小组自行训练模型、报告划分后未参与训练数据上的表现、解释每个主要预测模型，并说明结果的临床意义。每位成员都应能够回答完整项目的问题，而不只熟悉自己运行的脚本。生成式 AI 的使用还应按照课程要求如实声明。

同伴评价采用 1—9 分，前期汇总的四个维度为：按责任完成工作 15%，工作质量与深度 25%，组内沟通 30%，支持其他成员 30%。后两项合计 60%，因此共享结果表、互相审阅、轮换讲解和共同参加模拟答辩都应保留记录；仅仅把代码分别写完，不足以充分体现协作。

这些要求决定了我们的主线不应写成“我们试了很多算法”，而应回答三个连续问题：能否识别死亡风险，结果是否足够可靠，死亡之外的数据还能支持什么决策。

## 二、整场汇报的一句话主线

我们使用 ICU 入院后前 48 小时的不规则临床记录，先验证其能否改善院内死亡风险排序，再检查缺失、清洗、校准和病区差异是否会动摇结论，最后把同一数据扩展到住院时长预测和患者表型探索，并明确每项结果适合与不适合的应用范围。

## 三、建议围绕的三个主题

### 主题一：前 48 小时数据能否识别院内死亡风险

这一部分是主要预测任务。需要先说明行动者和行动：模型不是替代医生，而是在预先确定的预测时点为高级临床复核队列排序。随后展示静态信息、SAPS-I/SOFA、逻辑回归和梯度提升之间的公平比较，以及 6、12、24、48 小时时间价值。当前 48 小时梯度提升达到 AUROC 0.882、AUPRC 0.592、Brier 0.084；逻辑回归作为可解释基线，帮助说明复杂度带来的实际增量。

### 主题二：高分模型是否足够可信

这一部分承接 EDA，而不是把数据质量放在附录。七种清洗—填补方案的五折比较已经完成，没有发现稳定优于简单中位数方案的复杂处理；仅使用测量过程与静态信息的 48 小时梯度提升 AUPRC 已达到 0.447。最终死亡模型的 Bootstrap 95% AUROC 区间为 0.861—0.900，Platt 校准后的校准截距和斜率约为 0.045 和 1.030。验证集目标灵敏度 0.90 对应确认集灵敏度 0.92、每 100 人复核约 42 人、漏掉 21 名死亡患者。跨 ICU 类型压力测试还显示排序相对稳定，但平均预测风险会随病区病例组合发生偏移。

### 主题三：死亡之外，同一批数据还能支持什么决策

剩余住院时间作为第二个监督预测任务，回答队列还需要多少住院床日，而不是承诺个人出院日期。48 小时后个体预测由梯度提升取得 MAE 6.62 天，但低估总床日约 24.5%；另按验证集总量误差选择 smearing 岭回归后，总体偏差为 −0.77%，个体 MAE 增至 7.22 天，且长住院和病区误差仍明显。24 小时聚类是探索性扩展：四簇方案稳定性 ARI 为 0.980，但留出集轮廓系数只有 0.119，因此适合描述连续临床状态上的粗分组，不应宣称发现了四种疾病。

## 四、八页主线建议

| 页码 | 页面主题 | 页面只保留的主要结论 | 主要证据或图片 |
|---:|---|---|---|
| 1 | 临床问题与两项任务 | 在固定时间点支持死亡复核排序和住院资源估计 | 人群—时点—结局—行动流程图 |
| 2 | 数据结构与质量 | 数据稀疏、不规则，缺失与测量行为本身含有信息 | 数据时间轴、变量覆盖率、结局一致性图 |
| 3 | 划分、预处理与防泄漏 | 先划分再填补、缩放和选择模型，所有模型共享固定样本 | 完整训练流程图 |
| 4 | 死亡模型与时间价值 | 原始临床记录优于静态和评分参照，信息随时间增加 | AUROC/AUPRC/Brier 时间窗图与模型表 |
| 5 | 清洗与缺失敏感性 | 需要区分生理数值、缺失模式和数据处理带来的性能 | 填补—清洗实验矩阵、缺失模式消融 |
| 6 | 从概率到行动 | 阈值由漏诊代价和复核容量决定，AUC 不能替代工作点 | 校准曲线、阈值—灵敏度—告警负担图 |
| 7 | 第二任务与探索扩展 | 住院时长只能粗略预测；聚类稳定但边界重叠 | LOS 观察值—预测值图；聚类画像小图 |
| 8 | 建议与边界 | 可先研究性试点排序，不能承诺概率、出院日或新疾病实体 | “可以做/暂时不能做/还需验证”三列表 |

第 4—6 页是整场汇报的中心。回归满足第二个预测任务要求，聚类体现课程中无监督学习的应用，但在时间有限时不应挤压死亡模型的可靠性证据。

## 五、不同时长的取舍

如果只有 5 分钟，保留第 1、2、4、6、8 页；第 7 页只用一句话说明第二任务的结果，完整回归和聚类放入附录。如果有 8—10 分钟，按八页主线讲述，但第 5 页只展示最终选出的两到三种预处理方案。如果有 12—15 分钟，可以把第 7 页拆成“剩余住院时间”和“表型聚类”两页，并增加一页跨 ICU 类型验证。

无论时长如何，都不建议在主线中逐一介绍所有变量、所有聚类算法或所有超参数。主页面用于支持结论，完整指标表、PDP/置换重要度、亚组结果、假阴性画像、残差诊断和全部聚类候选放在答辩附录。

## 六、两个监督模型怎样直观解释

逻辑回归先计算加权和 `z=b₀+Σbᵢxᵢ`，再用 Sigmoid 函数把它转换成 0—1 的死亡概率。它适合解释每个变量如何把风险向上或向下推动。梯度提升按照 `Fₘ(x)=Fₘ₋₁(x)+ηhₘ(x)` 逐棵增加小树，每棵树修正前一轮剩余误差，因而能够表达阈值、非线性和变量交互。汇报中应强调复杂模型带来了多少实际增量，以及这份增量是否足以抵偿解释与校准成本，而不是笼统地说复杂模型更先进。

住院时长的岭回归在 `log(1+LOS)` 上拟合线性加权和，并用 L2 惩罚压缩不稳定系数；梯度提升回归用一组小树逐步修正连续结局残差。由于住院时长具有明显右长尾，直接反变换更接近中位数式预测，可能有利于个体 MAE，却容易低估队列总床日；后续 smearing 校正用于估计算术均值，两种读数必须分别报告。

## 七、后续实验与汇报制作顺序

第一步已经完成研究协议冻结：样本划分、时间边界、重复值处理、空 Parameter 规则、主要指标和模型选择规则均写入代码与元数据。当前测试集已经用于基线报告，因此统一称为“基线后重新锁定的确认集”，不描述为研究全过程中从未查看的盲测集。

第二步已经在 24 小时窗口完成七种死亡预处理的相同五折比较，并完成缺失模式消融。结果支持保留简单中位数方案；缺失指示对梯度提升没有形成一致增益，因此不再把 KNN 和迭代填补加入首轮主线。

第三步已经完成确认集 Bootstrap、校准截距与斜率、目标灵敏度 0.80/0.90/0.95 的工作点、ICU 类型、年龄和性别亚组、跨 ICU 类型训练—测试，以及死亡标签异常敏感性。主分析没有擅自修改官方死亡标签。

第四步已经把总住院时长改为 48 小时后的剩余住院时间，并分别选择个体 MAE 模型和队列总床日模型。聚类的多算法扩展暂缓；当前最有价值的后续工作是原始观测级临床范围审核、死亡模型成组解释，以及冻结所有幻灯片数字。

第五步进行至少两轮组内排练。第一轮检查每一页的结论能否由图表直接支持；第二轮轮换主讲人进行追问，保证每位成员能够解释数据处理、两个预测任务、指标、局限和最终建议。

## 八、优先准备的答辩问题

1. 为什么用 48 小时模型，而不是更早的 6 或 24 小时模型？
2. 为什么不只看 AUROC，AUPRC 和 Brier 分别补充了什么？
3. 逻辑回归与梯度提升谁是主模型，复杂度带来了多少增量？
4. 缺失模式 AUROC 较高是否说明模型在学习医生行为？
5. 阈值为什么不能直接使用 0.5 或 Youden J？
6. 每 100 人需要复核多少人，漏掉多少死亡病例？
7. 换到另一类 ICU 后，排序和概率是否都能保持？
8. 死亡时间与住院时间不一致会不会改变主结论？
9. 住院时长 R² 较低，为什么仍然保留这个任务？
10. 个体住院时间预测与队列床日估计为什么需要不同读数？
11. 聚类稳定性很高而轮廓系数很低，是否矛盾？
12. 哪些工作由组员完成，哪些环节使用了生成式 AI，如何复现？

## 九、措辞边界

建议使用“跨 ICU 类型内部可迁移性验证”，不要称为外部验证；使用“平均预测风险发生偏移”，不要说换病房后校准一定失效；使用“当前特征和模型未能区分部分死亡患者”，不要断言这些患者没有早期信号；使用“稳定但重叠的临床状态分组”，不要把聚类结果直接称为新疾病亚型。住院时长总量接近真实值时，还要同时展示病区误差，避免总体偏差因正负抵消而被误解为已经可以部署。

## 十、材料索引

- 完整研究报告：`RESEARCH_REPORT.md`
- 死亡预测：`Experiments/02_mortality_prediction_baseline/`
- 住院时长回归：`Experiments/04_length_of_stay_regression/`
- 患者表型聚类：`Experiments/05_patient_phenotype_clustering/`
- 死亡模型稳健性、工作点与校准：`Experiments/03_mortality_data_quality/`
- 48 小时后剩余住院时间：`Experiments/06_remaining_length_of_stay/`
- 课程内容：`CourseMaterials/`
